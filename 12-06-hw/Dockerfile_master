FROM mysql:8.0

# Копируем файлы конфигурации
COPY ./master/master.cnf /etc/mysql/conf.d/my.cnf
COPY ./master/master.sql /docker-entrypoint-initdb.d/start.sql

# Переменные окружения для настройки репликации
ENV MYSQL_ROOT_PASSWORD=12345

# Запускаем сервисы
CMD ["mysqld"]
